# === BACKUPS DE BASE DE DATOS ===
# Snapshots locales que pueden contener datos sensibles
backup_local_inicial.sql
*.sql.bak
*.dump
*.pgdump

# === SUPABASE CLI - carpetas internas ===
# Estado interno de la CLI, no aporta valor versionar
supabase/.branches/
supabase/.temp/

# === ARCHIVOS TEMPORALES DE TRABAJO ===
# Bloques de codigo, scripts intermedios, etc
_axc_*.txt
*.tmp
*.bak

# === SISTEMA OPERATIVO ===
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
.AppleDouble
.LSOverride

# === EDITORES E IDEs ===
# .vscode tiene configs personales por dev, no se versiona
.vscode/
.idea/
*.swp
*.swo
*~

# === LOGS ===
*.log
logs/

# === NODE / NPM (por si en el futuro se agrega) ===
node_modules/
npm-debug.log
yarn-error.log

# === ARCHIVOS DE ENTORNO (futuro) ===
# Si en algun momento se usa archivo .env con secretos
.env
.env.local
.env.*.local
# === SUPABASE STUDIO SNIPPETS ===
# Queries personales del SQL Editor, no son parte del schema
supabase/snippets/

# Backups con timestamp
backup_*.sql
